{
  "definitions": {
    "aluno": {
      "description": "Aluno do IPS",
      "type": "object",

      "properties": {
        "numero": {
          "description": "Número do aluno",
          "type": "integer",
          "pattern": "^[0-9]{9}$"
        },

        "nome": {
          "description": "Nome do aluno",
          "type": "string"
        },

        "morada": {
          "description": "Morada do aluno",
          "type": "string"
        },

        "nota": {
          "description": "Nota final do aluno",
          "type": "number"
        }
      },

      "required": ["numero", "nome", "nota"]
    },
    "turma": {
      "description": "Turma do IPS",
      "type": "object",

      "properties": {
        "id": {
          "description": "Id da Turma",
          "type": "integer"
        },
        "nome": {
          "description": "Nome da Turma",
          "type": "string"
        },
        "ano": {
          "description": "Ano de escolaridade",
          "type": "integer"
        }
      },

      "require": ["id", "nome", "ano"]
    }
  },

  "allOf": [
    { "$ref": "#/definitions/aluno" },
    {
      "type": "object",
      "required": ["turma_inscrito"],
      "properties": {
        "turma_inscrito": { "$ref": "#/definitions/turma" }
      }
    }
  ]
}

<html>
    <head>
        <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
        <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/js/bootstrap.min.js"></script>
        <link rel="stylesheet" href="../../css/site.css">

        <script>
            {{#hasReferences}}
            /*function loadValues(model,values,label,controlId) {
                var xhr = new XMLHttpRequest();
                xhr.open("GET", `../../../api/${model}/${values}`, true);
                xhr.onreadystage = function() {
                    if(xhr.readyState === 4 && xhr.status === 200) {
                        var control = document.getElementById(controlId);
                        var response = JSON.parse(this.responseText);
                        if(response.constructor === Array) {
                            response.map(row => {
                                var option = document.createElement("checkBox");
                                option.value = row[`${model}_id.toLowerCase()`];
                                option.textContent = row[label];
                                control.appendChild(option);
                            });

                        } else {

                            var option = document.createElement("option");
                            option.value = response[`${model}_id.toLowerCase()`];
                            option.textContent = response[label];
                            control.appendChild(option);
                        }
                    }
                }
            }*/

            window.onload = function() {
                {{#references1M}}
                    loadValues('{{model}}','{{values}}','{{label}}','label{{model}}value');
                {{/references1M}}
                {{#referencesMM}}
                    loadValues('{{model}}','{{values}}','{{label}}','label{{model}}value');
                {{/referencesMM}}
            }
            {{/hasReferences}}
        </script>
    </head>
    <body>
    {{>menu}}

        <div class="div">
            <form method="POST" action="/{{title}}">
                <div class="form-group">
                    {{#properties}}
                        <label>{{name}}:</label>
                        <input class="form-control" type="text"/>
                    {{/properties}}
                    {{#references1M}}
                        <label>{{label}}:</label>
                        <select id="label{{model}}value" class="form-control"/>
                    {{/references1M}}
                    {{#referencesMM}}
                        <label>{{label}}:</label>
                        <div id="label{{model}}value" class="form-control"/>
                    {{/referencesMM}}
                </div>

                <div class="form-group">
                    <input class="color btn" type="submit" value="Save"/>
                </div>
            </form>
        </div>
    </body>
</html>